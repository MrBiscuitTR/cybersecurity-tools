# network

Tools that operate at the host/transport/protocol level.

Fits here: TCP/UDP port scanning, banner grabbing, ping/ARP sweeps, packet
capture and crafting, DNS-over-the-wire probes, service fingerprinting.

Doesn't fit: HTTP application logic (see [../web](../web)) or passive OSINT
(see [../recon](../recon)).
