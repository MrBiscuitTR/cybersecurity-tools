# common

Shared helpers imported by the tool packages — no standalone tools live here.

Fits here: argparse/CLI scaffolding, logging setup, output formatting (tables,
JSON), input validators (IP/host/URL), rate limiting, file I/O helpers.
